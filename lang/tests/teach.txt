funky main() {
	let a = 97 in {
		print_i64(Wrapper~unwrap(Wrapper~wrap(a)));
	}
}

product Wrapper[T] {
	value <- T,
}

teach[T] Wrapper[T] {
	funky wrap(value <- T) -> Self {
		Wrapper {
			value <- value,
		}
	}

	funky unwrap(self <- Self) -> T {
		self.value
	}
}
