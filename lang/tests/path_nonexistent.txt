funky main() {
	print_i64(outer~inner~identity~bingus);
}

mod outer {
	pub mod inner {
		pub funky identity[T](value <- T) -> T {
			value
		}
	}

	funky bingus(binglings <- i64) -> i64 {
		inner~identity(binglings)
	}
}
