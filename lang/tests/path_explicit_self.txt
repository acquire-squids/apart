funky main() {
	print_i64(outer~bingus(97));
}

mod outer {
	mod inner {
		pub funky identity[T](value <- T) -> T {
			value
		}
	}

	pub funky bingus(binglings <- i64) -> i64 {
		outer~inner~identity(binglings)
	}
}
